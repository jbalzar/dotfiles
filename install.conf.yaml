- defaults:
    link:
      relink: true
- shell:
  - [git submodule update --init --recursive, Installing submodules]
- link:
    ~/.local/share/code-server:
      create: false
      path: code-server
      force: true